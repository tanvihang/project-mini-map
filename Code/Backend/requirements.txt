# Mini-Map Backend — locked third-party dependencies.
# All versions are pinned to an installed, mutually-compatible set
# (resolved against google-adk / fastmcp, which drive the web stack floor).

# --- Web framework (async) ---
fastapi==0.136.3
uvicorn[standard]==0.48.0

# --- Validation / settings ---
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# --- Data layer (async Mongo driver, per docs/engineering standards) ---
# NOTE: Motor is in maintenance/EOL; flagged for a future migration to the
# native PyMongo async API. Kept here to comply with the data-layer standard.
motor==3.7.1
pymongo==4.10.1

# --- HTTP client (async) ---
httpx==0.28.1

# --- Google Gemini / Agent stack ---
google-genai==1.75.0
google-adk==2.1.0

# --- MCP server framework for the geo service ---
fastmcp==3.3.1

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.0

# --- Lint / format (Google style) ---
ruff==0.8.4
black==24.10.0
